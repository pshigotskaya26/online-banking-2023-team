import { Dispatch } from 'redux';
import { TransfersActions } from '../types/transfers';

export const getCredits = () => {
  return (dispatch: Dispatch<TransfersActions>) => {
    try {
    } catch (e: unknown) {
      if (e instanceof Error) {
      }
    }
  };
};
